# Brief v1.0 Implementation Plan — Production Ready

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task.

**Goal:** Stable, battle-tested, recommended to other teams. Defaults are good. Edge cases handled. Spec published.

**Prerequisites:** v0.5 shipped. 4+ weeks of daily use by WTL team (humans + agents). Scoring/formatting tuned from real feedback.

---

### Task 1: Conflict detection

**Files:**
- Create: `src/store/conflicts.ts`
- Modify: `src/cli/sync.ts`
- Test: `src/__tests__/conflicts.test.ts`

- [ ] **Step 1: Write failing test**

Test: when two sources produce conflicting priorities for the same item ID, the renderer flags the conflict visually:
```markdown
- issue:project-c#45 [status: active] ⚠️ CONFLICT
  scope says: NOW (score 39)
  override says: removed
```

- [ ] **Step 2: Implement conflict detection**

During sync, if multiple sources reference the same item ID with different priorities/statuses, flag it. Higher-priority source wins but the conflict is visible.

- [ ] **Step 3: Run tests**

- [ ] **Step 4: Commit**

---

### Task 2: Stale priority auto-detection

**Files:**
- Modify: `src/cli/sync.ts`
- Modify: `src/cli/doctor.ts`

- [ ] **Step 1: Implement staleness warnings**

During sync, if a priority item hasn't changed in 14+ days, flag it:
```markdown
- issue:project-a#89 [status: active] ⚠️ STALE (unchanged 14 days — still relevant?)
```

`brief doctor` shows stale items with suggestion to review or archive.

- [ ] **Step 2: Commit**

---

### Task 3: Assignment suggestions

**Files:**
- Modify: `src/cli/doctor.ts`

- [ ] **Step 1: Implement workload analysis**

`brief doctor` shows workload per person:
```
People:
  alice    3 items assigned
  bob      0 items assigned ← underutilized
  bot-1    8 items assigned ← overloaded
```

Suggests rebalancing when distribution is skewed.

- [ ] **Step 2: Commit**

---

### Task 4: Weekly digest generation

**Files:**
- Create: `src/cli/digest.ts`
- Modify: `src/index.ts`

- [ ] **Step 1: Write failing test**

Test: `brief digest` generates a markdown summary of the week's changes.

- [ ] **Step 2: Implement digest command**

```bash
brief digest              # generate weekly summary
brief digest --json       # machine-readable
brief digest --period 7d  # custom period
```

Reads agent-log.md + decisions.md + git log of .brief/ repo. Outputs:
- Decisions made this week
- Items resolved
- Items added
- People assignments changed
- Stale items flagged

- [ ] **Step 3: Run tests**

- [ ] **Step 4: Commit**

---

### Task 5: Pre-commit validation hook

**Files:**
- Create: `src/cli/hook.ts`

- [ ] **Step 1: Implement hook installer**

```bash
brief hook install    # adds pre-commit hook to .brief/ repo
brief hook remove
```

The hook runs `brief validate` before each commit. Rejects commits with broken frontmatter.

- [ ] **Step 2: Commit**

---

### Task 6: Comprehensive test suite

**Files:**
- Create: `src/__tests__/sync.test.ts`
- Create: `src/__tests__/assign.test.ts`
- Create: `src/__tests__/decision.test.ts`
- Create: `src/__tests__/urgent.test.ts`

- [ ] **Step 1: Write tests for every command**

Target: 30+ tests covering:
- Sync with mock sources (success, failure, timeout, conflict)
- Assign (create, update, duplicate)
- Decision (append, date grouping)
- Urgent (with/without notification config)
- Override (add, remove, boost, clear)
- Graph (add link, query, cycles)
- Check (all 4 exit codes)
- Status (stale threshold)
- Edge cases (empty .brief/, missing config, broken YAML)

- [ ] **Step 2: Run all tests, verify 100% pass**

- [ ] **Step 3: Commit**

---

### Task 7: Spec finalization + governance

**Files:**
- Modify: `SPEC.md`

- [ ] **Step 1: Review and update spec**

Based on 4+ weeks of real usage:
- Update examples with real patterns that proved useful
- Document patterns that didn't work
- Finalize file formats (lock brief_version: 2 if changed)
- Add FAQ section

- [ ] **Step 2: Publish spec**

Consider: brief-spec.org or just keep at github.com/fureworks/brief/SPEC.md.
Add contributing guidelines for the convention itself (not just the CLI).

- [ ] **Step 3: Commit and tag v1.0.0**

```bash
npm version 1.0.0
git tag v1.0.0
npm publish
gh release create v1.0.0
```
